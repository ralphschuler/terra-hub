{
  "name": "@terra-hub/rules-engine",
  "version": "0.1.0",
  "description": "TerraHub Rules Engine - Condition evaluation and action triggering",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "echo \"No tests yet\"",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist",
    "formatter:write": "prettier --write \"src/**/*.{ts,js,json}\" \"*.{ts,js,json,md}\"",
    "liners:fix": "pnpm formatter:write"
  },
  "dependencies": {
    "@terra-hub/config-schema": "workspace:*"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.3.0"
  },
  "files": [
    "dist",
    "README.md"
  ]
}
