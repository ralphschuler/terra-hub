{
  "name": "@terra-hub/config-schema",
  "version": "0.1.0",
  "description": "TerraHub Configuration Schemas - JSON & TypeScript schemas for ports, nodes, rules",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "echo \"No tests yet\"",
    "lint": "echo \"No lint configured yet\"",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "files": [
    "dist",
    "schemas",
    "README.md"
  ]
}
