{
  "name": "@terra-hub/simulator",
  "version": "0.1.0",
  "description": "TerraHub Simulator - Node-based chain and rule testing",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "test": "echo \"No tests yet\"",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist",
    "formatter:write": "prettier --write \"src/**/*.{ts,js,json}\" \"*.{ts,js,json,md}\"",
    "liners:fix": "pnpm formatter:write"
  },
  "dependencies": {
    "@terra-hub/protocol": "workspace:*",
    "@terra-hub/rules-engine": "workspace:*",
    "@terra-hub/config-schema": "workspace:*"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.10.0"
  },
  "files": [
    "dist",
    "README.md"
  ]
}
