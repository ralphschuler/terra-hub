{
  "name": "terra-hub",
  "version": "0.1.0",
  "private": true,
  "description": "TerraHub - Open Terrarium Control Hub",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ralphschuler/terra-hub.git"
  },
  "workspaces": [
    "packages/*",
    "apps/*",
    "tools/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "formatter:write": "prettier --write .",
    "liners:fix": "pnpm formatter:write"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.3.0"
  }
}
